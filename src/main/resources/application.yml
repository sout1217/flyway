spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flyway?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # ???
    generate-ddl: false
    open-in-view: false

  flyway:
    url: jdbc:mysql://localhost:3306/flyway?serverTimezone=UTC&characterEncoding=UTF-8
    user: root
    password:
    enabled: true
    baseline-on-migrate: true # spring boot 2.x ([spring boot2] auto create version table)
    baseline-version: 3.0.7